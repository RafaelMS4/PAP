{
  "name": "fullstack-app",
  "version": "1.0.0",
  "description": "Full-stack app with React + Vite and Express",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "cd client && npm run build",
    "preview": "cd client && npm run preview",
    "client": "cd client && npm run dev",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
